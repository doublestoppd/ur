# No dependencies. The only generated artifact is dist/ur-compiler.html, the
# single-file distribution, which IS committed: tests/run.js rebuilds it after
# every green suite so it always matches the sources.
node_modules/
*.log

# Never commit real exports or compiled workbooks: they contain PHI.
*.xlsx
*.xls
*.csv
!tests/fixtures/**
